#include<stdio.h>

void main()
{

	int n, rev=0, d;
	printf("\nEnter a number : ");
	scanf("%d", &n);

	int p=n;
	while(n != 0)
	{
		d = n%10;
		rev = rev*10+d;
		n=n/10;
	}

	if(p==rev)
		printf("\nPalindrome\n");
	else
		printf("\nNot palindrome\n");
	
}
