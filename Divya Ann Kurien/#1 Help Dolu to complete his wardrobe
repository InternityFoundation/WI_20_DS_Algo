include<stdio.h>
void main()
{
  int a[10],n, res;
  printf("Enter size of array");
  scanf("%d", &n);
  printf("\nEnter the elements");
  for(int i=0;i<n;i++)
  scanf("%d", &a[i]);
  res = a[0];
  for(int i=1;i<n;i++)
  res ^= a[i];

  printf("\nThe non-repeating element is %d\n", res);
}
