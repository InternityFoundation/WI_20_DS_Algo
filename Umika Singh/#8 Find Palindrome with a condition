#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main() {
	// your code goes here
  int n;
  cin>>n;
  int num=n, newnum=0;
  while(n>0)
  {
      int d=n%10;
      newnum=newnum*10+d;
      n=n/10;
  }
  if(num==newnum)cout<<"true";
  else cout<<"false";
	return 0;
}
