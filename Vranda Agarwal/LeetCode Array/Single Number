class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int temp=nums[0];
        for(int i=1;i<=nums.size()-1;i++)
        {
            temp=temp^nums[i];
        }
        return temp;
    }
};
