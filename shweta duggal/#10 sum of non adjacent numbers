  
#include <iostream.h>

using namespace std;

int main()
{
    int a;
    
    cin>>a;
    
    int a[a];
    for(int i=0;i<a;i++)cin>>a[i];
    
    {

    int ans=0;
    }

    for(int i=0;i<a;i++)
    {
        int j=i+2,sum=0;
        while(j<a)
        {
            sum=a[i]+arr[j];
            ans=max(ans,sum);
            sum=0;
            j++;
        }
    }

    cout<<ans;

    return 0;
}
